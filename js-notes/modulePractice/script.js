// Imported modules are placed at the top, so that the browser will load them first before preceeding through the code. Note that a live-server environment is needed to run local modules.

import { firstObject } from "./modules/export.js";
console.log(firstObject);

// console.log(totalCost.price + totalCost.tax);
// console.log(totalCost.default);
